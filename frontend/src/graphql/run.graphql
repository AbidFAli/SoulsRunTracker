mutation CreateRun($input: RunCreateInput!){
  createRun(run: $input){ #i know the gameId
    id
    name
    descriptionUrl
    completed
    deaths
    cycles{
      id
      level
      completed
      bossesCompleted{
        id
        completed
        instance{ #look up instance information in another request
          id
          bossId
          locationId
        }
      }
    }
  }
}

fragment UserRuns on Run{
  id
  name
  completed
  gameId
  currentCycle
  deaths
  character{
    id
    name
    level
  }
}

query GetUserRuns($where: RunWhereInput!, $pagination: RunPaginationInput!){
  runs(where: $where, pagination: $pagination){
    edges{
      ...UserRuns
    }
    pageInfo{
      ...PageInfoFragment
    }
  }
}




# query GetBossCompletions{
#   run{ #i know the gameId
#     id
#     name
#     descriptionUrl
#     completed
#     cycles{
#       id
#       level
#       completed
#       bossesCompleted{
#         completed
#         instance{ #look up instance information in another request
#           id
#           bossId
#           locationId
#         }
#       }
#     }
#   }
# }