type Run{
  id: ID!
  name: String
  descriptionUrl: String
  completed: Boolean
  gameId: ID
  userId: ID
  game: Game
  cycles: [Cycle!]
  character: Character
}





"""

#TODO arguments. What if I have a complex where, can I pass that as child args? Should I pass as parent value?


mutation CreateRun{
  run{ #i know the gameId
    id
    name
    descriptionUrl
    completed
    cycles{
      id
      level
      completed
      bossesCompleted{
        completed
        instance{ #look up instance information in another request
          id
          bossId
          locationId
        }
      }
    }
  }
}


query GetGameAndBosses{
  game{
    id
    gameLocation{
      id
      order
      location{
        name
      }
      bossInstance{
        id
        boss{
          id
          name
        }
        locationId
        order
      }
    }
  }
}

query GetBossCompletions{
  run{ #i know the gameId
    id
    name
    descriptionUrl
    completed
    cycles{
      id
      level
      completed
      bossesCompleted{
        completed
        instance{ #look up instance information in another request
          id
          bossId
          locationId
        }
      }
    }
  }
}
"""








